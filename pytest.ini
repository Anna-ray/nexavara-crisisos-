[pytest]
testpaths = tests
python_files = test_*.py
addopts = --maxfail=1 --strict-markers --cov=adapters --cov=agents --cov=services --cov=messages --cov=nexavara --cov-report=term-missing --cov-report=html
markers =
    load: load and performance tests
    security: security validation tests
